<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose 로딩 전 윈도우 배경용. 앱 UI 자체는 Compose(MaterialTheme)로 그린다. -->
    <style name="Theme.ExternalCamera" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
